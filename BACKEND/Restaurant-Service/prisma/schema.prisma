generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// RESTAURANT MODEL
model Restaurant {
  id      String   @id @default(uuid())
  name    String  
  address String
  owner   String
  member  String[]
  food    String[]
}